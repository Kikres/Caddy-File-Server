Step 1:
Replace the path to the source folder in "Caddyfile_fs"

Step 2:
Right klick on "caddy.exe" and open in terminal. Run the following command as a single row:
.\caddy.exe run --config .\Caddyfile_fs --watch
